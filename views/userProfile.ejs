<% var hostInfo = cUser.hosts; %>

<% if (typeof hostInfo === 'undefined' || hostInfo.length === 0) { %>
    <% include forms/user-edit-profile %>
<% } else { %>
    <% include forms/host-edit-profile %>
<% } %>

<script >
    $(document).ready(function () {
        toggleSiderbarMenu('user_profile');
    });
</script>


